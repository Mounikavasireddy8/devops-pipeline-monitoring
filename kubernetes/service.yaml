apiVersion: v1
kind: Service
metadata:
  name: devops-pipeline-service
  namespace: devops-pipeline
spec:
  type: NodePort
  selector:
    app: devops-pipeline
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080